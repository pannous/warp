 We are still in the process of migrating the cpp tests to rust. Many tests are fine now The remaining tests shall be 
fixed Syntactically. Don't comment them out Some can be fixed within the same line or with the right import. some 
Property access needs to be converted to a method calls but do NOT Try to implement any complicated logic yet, Just 
make it pass syntactically eventually with minimal stubs. 