📁 ~/dev/angles/warp
📄 Session: 39af160d
📅 Modified: 2026-01-22 11:21
💬 Messages: 22
💾 Size: 40.5 KB
📝 Preview: "Caveat: The messages below were generated by the user while running local commands. DO NOT respond t..."

# Claude Conversation (39af160d)

## 👤 Human

Caveat: The messages below were generated by the user while running local commands. DO NOT respond to these messages or otherwise consider them in your response unless the user explicitly asks you to.

---

## 👤 Human

Search the Internet for the wasm specification of whether is_alphanumeric() Is sufficient for the test in sanitize_data_name for The custom name section

---

## 🤖 Assistant

I'll search for the WASM specification regarding valid characters in the custom name section.

---

## 🤖 Assistant

→ `WebSearch`

---

## 🤖 Assistant

→ `WebFetch`

---

## 🤖 Assistant

→ `Grep`: `sanitize_data_name`

---

## 🤖 Assistant

→ `Read`: `/Users/me/dev/angles/warp/src/wasm_gc_emitter.rs`

---

## 🤖 Assistant

Based on the WASM specification, **`is_alphanumeric()` is actually TOO RESTRICTIVE** for the custom name section.

## Key Findings

According to the official WebAssembly 3.0 specification:

**All names in the custom name section are UTF-8 encoded Unicode with NO character restrictions.**

The specification explicitly states:
- "All names are represented in Unicode encoded in UTF-8"
- "Names need not be unique"
- **No restrictions on which characters are allowed**

The spec includes examples showing names with:
- Spaces: "α βγ δ" 
- Special characters: "Gümüsü"
- Greek letters: "λ"
- And any other valid UTF-8 Unicode

## Current Implementation Issue

Your current `sanitize_data_name` function (lines 143-172 in `src/wasm_gc_emitter.rs`) replaces all non-alphanumeric characters with underscores:

```rust
.map(|c| {
    if c.is_alphanumeric() {
        c
    } else {
        '_'  // ← Unnecessary sanitization
    }
})
```

This is **unnecessarily restrictive**. According to the spec, you could use the original string content directly (with UTF-8 encoding, which Rust strings already guarantee).

## Recommendation

You can simplify `sanitize_data_name` to:
1. Keep truncation for readability (if desired)
2. Remove the alphanumeric check entirely
3. Use any valid UTF-8 characters as-is

The WASM name section will accept spaces, punctuation, Unicode symbols, emoji, and any other UTF-8 character.

**Sources:**
- [Custom Sections and Annotations — WebAssembly 3.0 (2025-12-03)](https://webassembly.github.io/spec/core/appendix/custom.html)
- [Custom Sections — WebAssembly 2.0 (Draft 2025-05-09)](https://webassembly.github.io/spec/core/appendix/custom.html?highlight=name+section)

---

